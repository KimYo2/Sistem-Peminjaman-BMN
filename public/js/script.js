
// Toast Notification
function showToast(message, type = 'success') {
    // Simple toast implementation if needed later
    console.log(type.toUpperCase() + ': ' + message);
    alert(message); // Fallback for now
}
